.env.local
.env*
!.env.example

# Server-side identity store — accounts, sessions, password hashes.
.data/

# Editor and build artefacts
.DS_Store
*.tsbuildinfo

node_modules/
.next/
out/

# Agent worktrees — each is its own git repo; committing one accidentally
# records it as a gitlink with no .gitmodules entry, which is what "Failed to
# fetch one or more git submodules" during a Vercel clone was coming from.
.claude/worktrees/
