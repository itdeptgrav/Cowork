import { SettingsIndex } from "@/components/features/admin/SettingsIndex";

export const metadata = { title: "Admin settings — Cowork" };

export default function Page() {
  return <SettingsIndex />;
}
