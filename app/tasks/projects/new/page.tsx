import { NewProjectForm } from "@/components/features/projects/NewProjectForm";

export const metadata = { title: "New project — Cowork" };

export default function Page() {
  return <NewProjectForm />;
}
