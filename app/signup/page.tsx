import { SignUpForm } from "@/components/features/auth/SignUpForm";

export const metadata = { title: "Create your organisation — Cowork" };

export default function Page() {
  return <SignUpForm />;
}
